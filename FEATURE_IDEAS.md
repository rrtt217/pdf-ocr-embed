# FEATURE_IDEAS — 新功能脑暴清单

> 分支：`feature-ideas`。本文档是「接下来还可以添加什么」的候选清单——
> **只记录想法，不改变任何现有行为**。每条想法在真正动手前，建议单独开分支、
> 小步提交，并补充测试与文档。

## 背景与约束（想新功能前先记住）

来自 `AGENTS.md` 的硬性约定，任何新功能都必须兼容：

- `OcrBlock.bbox` 全程为**原始像素空间整数** `[x1,y1,x2,y2]`（左上原点）。
- adapter 一律返回归一化 `OcrPage`，其余代码不得依赖某一引擎的原始输出。
- 密钥/端点只经外部配置进入：`backend/config.py::resolve()`（TOML + OCR_* 环境变量
  最高优先级覆盖），绝不硬编码、绝不重新引入 .env/JSON 配置。
- `max_tokens` 必须 < 32768。
- 前端无构建步骤，直接改 `frontend/*`；新增 UI 文案需同步
  `frontend/i18n.js` 的中英两套。
- 目前**没有测试套件**——为新逻辑（尤其是解析器与坐标映射）补 pytest 应该是
  优先级最高的工程投资（见下方 #21）。

## A. OCR 正确性与质量

1. **置信度审阅视图**（S / 高价值 / 未做）
   `OcrBlock.conf` 一直存在，但前端从未消费。按置信度给块着色/排序、一键过滤
   「低于阈值」的块、跳转审阅，能显著降低人工纠错成本。
   涉及：`frontend/app.js`（buildBlockEditor/overlay）+ 少量 i18n。

2. **图像预处理提升识别率**（M）
   OCR 前自动纠偏（deskew）、去噪、对比度/二值化增强，逐页可开关、可预览效果。
   扫描件倾斜与发灰是识别率的大敌。涉及：`backend/pdf_processing.py` 渲染路径，
   加 PIL 预处理步骤（保持 bbox 像素不变式：预处理必须在 OCR 前、且输出尺寸不变
   或在 adapter 侧换算——建议保持原尺寸以免破坏坐标约定）。

3. **LLM 后处理校对（可选）**（M）
   复用已有的 API 配置做「识别后纠错/规范化」：修正明显错字、去除断字、统一标点，
   bbox 与块结构不动。对正式出版/存档场景加成很大，但会多一轮 API 调用与费用，
   应为显式可选项。涉及：`backend/sources` 旁新增后处理模块。

4. **多引擎融合（投票/仲裁）**（L）
   同一页跑两个 adapter（如 tesseract + API 模型），按块交并比做共识投票，取
   置信度更高者。正确率提升明显，但成本翻倍——UI 必须明示双引擎调用与开销，
   且只能作可选模式。涉及：`backend/ocr_service.py` + 页面数据合并逻辑。

5. **表格结构化 / 公式 LaTeX 增强**（L）
   现有 `kind=table` 只有按行文本。可检测表格线还原单元格结构（输出 markdown
   表格、并嵌入对齐更好的文本层）；`kind=equation` 可尝试转 LaTeX。对技术文档
   价值高，但受引擎能力与 PDF 复杂度制约，建议先做易验证的线检测原型。

## B. 编辑与审阅体验

6. **块操作增强**（M）
   合并/拆分文本块、新增块、删除块、拖拽或键盘微调 bbox。当前只能改文字，
   bbox 错误只能原样嵌入。涉及：`frontend/app.js` overlay 交互 +
   `POST /api/pages/{job}/{i}` 已具备更新通道，主要工作在前端与必要的后端校验。

7. **全文查找与替换**（M）
   跨页检索 ocr 文本、高亮命中块并跳转；支持全量/单页替换（替换后走既有更新接口，
   可扩展 `/api/pages` 支持跨页批量补丁以维持原子性）。

8. **编辑历史 undo/redo**（M）
   以快照或 diff 方式记录每次块编辑，前端栈实现即可，后端可加
   `PATCH /api/pages/{job}/{i}/blocks/{bi}` 细粒度接口避免整页覆盖。

9. **无障碍与快捷键补强**（S）
   已有 `←/→`、`Ctrl/⌘+Enter` 等，可继续加 Tab 块间跳转、键盘改 conf 过滤、
   键盘可达性（ARIA）审计。

## C. 批处理与工作流

10. **批量上传 + 队列 + 打包下载**（M）
    一次拖入多个 PDF 成任务队列，逐个 OCR，完成后统一打包 zip；
    复用现有多任务列表（服务端为唯一数据源）。

11. **任务持久化到磁盘**（M）
    任务已在内存中、重启即失（README 说明与限制）。把 job 状态落盘到
    `work/<job_id>/job.json`，启动时恢复，并与现有 cleanup 的「孤儿文件」
    逻辑联动（引用关系需一并持久化）。价值高：长文档 OCR 中断后不必重来。

12. **选定页范围 / 单页重跑**（S）
    现有 retry 只补失败/缺失页；新增「只 OCR 第 N~M 页」与「选定已成功页强制
    重跑」（对调 prompt、换引擎后的 A/B 试跑非常有用）。涉及
    `backend/ocr_service.py` 与 upload/retry 参数。

13. **OCR 结果缓存**（S / ✅ 已实现于 `feat/01-tests-ocr-cache`：`backend/ocr_cache.py`，hash 键 + TTL + `/api/cache`）
    按「源 PDF hash + 页码 + 预处理参数 + 引擎/参数」缓存 `OcrPage`。重复跑
    同一文件不再重复调用 API（省时省钱），对大文档反复调试价值突出。注意缓存
    生命周期要纳入 cleanup。

14. **监视文件夹自动处理**（M）
    本地模式指定一个目录，新增 `.pdf` 自动走「OCR → 校验 → 嵌入 → 输出」，
    适合扫描仪/打印机工作流。配置进 TOML，前端只显示状态。

15. **无头 CLI 模式**（S）
    `python -m backend.cli in.pdf --adapter tesseract --pages 1-20`，脚本化
    批量处理，复用 ocr_service/pdf_processing，WebUI 与 CLI 共享同一后端逻辑。

16. **完成通知 Webhook**（S）
    OCR/嵌入完成后向可配置 URL POST 通知（进度、结果下载地址），适配自动化
    管道。设置走 TOML，默认关闭。

## D. 输出与校验

17. **嵌后自动校验 + 质量报告**（M）
    嵌入后立即用 PyMuPDF 抽取嵌后文本，与 OCR 源文本对比给出页级覆盖率/字数/
    置信度分布，UI 展示报告。这是「信不信这个 PDF」的关键闭环，几乎无人做过，
    差异化大。涉及：`backend/pdf_processing.py` + 新校验模块 + 前端报告视图。

18. **输出 PDF 优化选项**（S）
    嵌入时可选：图片降采样/重压缩、线性化（web 快开）、PDF/A 合规。PyMuPDF
    `doc.save(...)` 已支持大部分参数，主要是暴露开关与预设。

19. **文本层导出扩展**（S）
    现有 `downloadDataset()` 仅导出内部 JSON。增加导出纯文本（txt，按阅读序
    拼接）与 markdown，并可选「只导出低置信区段」供人工校对。

20. **可见文本层预览开关**（S）
    调试视图把 `render_mode` 从 3 切到 0，直接看见嵌入文字与原始扫描图的
    对位偏差，作为校对/验收工具（仅预览不下发）。

## E. 运维与工程质量

21. **pytest 测试脚手架（P0）**（S / ✅ 已实现于 `feat/01-tests-ocr-cache`：`tests/` 49 用例 + `requirements-dev.txt`）
    优先覆盖 `sources/*` 的解析器与 `base.normalize_bbox` 等坐标纯函数，
    再覆盖 embed 的 y 轴翻转与缩放。这是所有后续功能的安全网。

22. **调用侧自动重试 + 限速**（S）
    `httpx` 对 429/5xx/瞬时网络错误做指数退避重试；per-adapter 速率上限与
    `concurrency` 联动。现有并发参数已经有了，缺的是失败自愈。

23. **Adapter 自检面板**（S）
    `/api/health` 扩展：tesseract 版本检测（`tesseract --version`）、API
    endpoint 连通性/key 有效性一次点测，把「配置错在哪」直接告诉用户。

24. **结构化日志 + 一键导出**（S）
    后端日志已可看（`/api/logs` + Logs 按钮），可加 JSON 结构化输出与
    「导出本次任务日志」按钮，方便贴进 issue。

## F. 分发与形态

25. **Tauri 桌面打包**（L）
    DESIGN.md 里预留的 optional 方向：WebUI 直接复用，优点是无浏览器依赖 +
    本地文件访问天然方便（配合 #14 监视文件夹很合适）。成本在打包与升级通道。

26. **PWA / 离线增强**（M）
    纯前端静态资源做 service worker 缓存，弱网环境体验提升；注意与
    NoCacheStaticMiddleware 的冲突需要专门处理。

27. **认证与多用户**（L / 谨慎）
    当前默认绑定本机使用。若多用户，先加最轻的 Basic Auth/单 token（TOML 配置），
    完整账号体系短期不建议——快速安全省事优先。

## 建议的优先级（供讨论，非承诺）

| 批次 | 想法 | 理由 |
| ---- | ---- | ---- |
| P0 快赢 | #21 测试、#13 缓存、#12 页范围/单页重跑、#22 重试限速、#1 置信度视图、#18 输出优化 | 成本低、不碰核心不变式、立竿见影 |
| P1 价值主力 | #11 任务持久化、#10 批量、#17 嵌后校验报告、#2 预处理、#6 块操作 | 覆盖核心用户流程的明显短板 |
| P2 体验/扩展 | #7 查找替换、#8 历史、#15 CLI、#16 Webhook、#19/#20 导出与调试、#23/#24 运维 | 视反馈再排 |
| P3 大块头/看场景 | #3 LLM 校对、#4 多引擎融合、#5 表格/公式、#14 监视文件夹、#25 桌面打包、#26 PWA、#27 账号 | 价值实但成本/风险高，宜先小原型 |

## 落地流程

1. 每个想法单独开分支（如 `feat/01-conf-review`），小步提交、可独立合入。
2. 有纯逻辑的优先写测试（#21 先行）。
3. 改 UI 文案同步 `frontend/i18n.js` 中英两套。
4. 完成后更新 README.md / README.en.md / DESIGN.md 相应章节。
5. 合入前对照 `AGENTS.md` 的检查清单过一遍。
