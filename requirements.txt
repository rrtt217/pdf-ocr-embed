# Core backend
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.6.0
httpx>=0.27.0

# OCR core: OCRmyPDF (tesseract/gs system packages required; see README).
# The unlimited engine ships as the backend.ocrmypad plugin.
ocrmypdf>=17.11.0

# Page preview rendering (WebUI editor)
PyMuPDF>=1.24.0

# unlimited engine: math/LaTeX normalization
pylatexenc>=2.10

# TOML config
tomli>=2.0.0; python_version < "3.11"
tomli-w>=1.0.0
