# Core backend
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.6.0
httpx>=0.27.0

# OCR core: OCRmyPDF (system deps: tesseract only for the Tesseract engine;
# ghostscript is OPTIONAL since 17.0 — needed only for PDF/A output; see README).
# The unlimited engine ships as the standalone ocrmypdf_unlimited plugin
# (ocrmypdf_unlimited/), loaded by dotted module name or its entry point.
ocrmypdf>=17.11.0

# PDF access is deliberately minimal (see backend/pdf_processing.py): both of
# these are already mandatory OCRmyPDF dependencies, declared here because the
# backend imports them directly.
#   pypdfium2 — page rendering, page geometry/count, text extraction
#   pikepdf   — the one editing operation (dropping pages for a partial embed)
# PyMuPDF is NOT used: it is AGPL-3.0/commercial and pypdfium2 covers its role.
pypdfium2>=5.0.0
pikepdf>=10

# unlimited engine: math/LaTeX normalization
pylatexenc>=2.10

# TOML config
tomli>=2.0.0; python_version < "3.11"
tomli-w>=1.0.0

# Per-user data/config/log directories (required when frozen/packaged; see
# backend/paths.py).  In a source checkout the repo-relative layout is kept.
platformdirs>=4.0
